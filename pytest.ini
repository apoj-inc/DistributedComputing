[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    smoke: Fast non-DB smoke checks for built binaries.
    integration: Binary-level integration checks that invoke built executables.
